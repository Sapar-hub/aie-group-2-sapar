# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# Large artifacts (trained weights — not committed)
artifacts/yolo/
artifacts/rcnn/
artifacts/detr/
artifacts/models/*.pt
artifacts/models/*.pth

# Pretrained weights (commit only small ones, <10MB)
# artifacts/pretrained/*.pt  — add to .gitignore if too large

# Synthetic data (generated, not committed)
data/images/train/
data/labels/train/

# OS
.DS_Store
Thumbs.db
*.cache
