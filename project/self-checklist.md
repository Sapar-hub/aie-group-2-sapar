# Самопроверка проекта (self-checklist)

| # | Критерий | Статус | Где смотреть / комментарий |
|---|----------|--------|---------------------------|
| 1 | Сервис запускается по инструкциям из `README.md` и работает | ✅ | `README.md` раздел 4.5: `gost-detect`, `python -m src.api.main`, `docker compose up -d`. `pyproject.toml:27` entry point. `Dockerfile` — multi-stage, HEALTHCHECK. `docker-compose.yml` — healthcheck, volume, restart. `src/api.main:265-271` — uvicorn на 0.0.0.0:8000. |
| 2 | Endpoint `/predict` использует **реальную модель**, а не заглушку | ✅ | `src/api/main.py:61-95` — `load_model()` загружает YOLO из `artifacts/models/best.pt` (symlink → `yolo/v4/weights/best.pt`). Fallback: RCNN (`artifacts/models/rcnn_best.pth`), затем CV baseline (`src/models/cv_baseline.py`). Все три — реальные модели. |
| 3 | Есть EDA и хотя бы один эксперимент с метриками | ✅ | `notebooks/exp01_eda_baseline.ipynb` — EDA (PPI, размеры, aspect ratio). `exp03_cv_baseline.ipynb` — CV baseline. `exp04_yolo_experiments.ipynb` — YOLO. `exp05_faster_rcnn.ipynb` — RCNN. `exp06_hybrid.ipynb`. `artifacts/metrics/eda_summary.json` + `yolo_v4_results.json`. |
| 4 | Есть baseline и улучшенная модель, есть **сравнение по метрикам** | ✅ | `src/evaluation/comparison.py` — сводное сравнение 4 моделей. `artifacts/metrics/comparison_results.json`. Итоговая таблица: CV (F1 0.727), YOLO v3 (F1 0.871), YOLO v4 (F1 0.754), Hybrid v4 (F1 0.712), RCNN v4 (F1 0.627). `README.md` раздел 5, `report.md` раздел 5. |
| 5 | Код не свален в один ноутбук: есть структура в `src/` | ✅ | `src/data/` — loader, synthetic, image_quality. `src/models/` — cv_baseline, train_yolo, rcnn_model. `src/evaluation/` — metrics, comparison, evaluate_*. `src/hybrid/` — refiner. `src/api/` — main.py, schemas.py. `tests/` — 18 тестов (test_metrics.py, test_models.py, test_pipeline.py). |
| 6 | Есть Dockerfile **или** понятный сценарий развёртывания без Docker | ✅ | `Dockerfile` — multi-stage (builder → runtime), python:3.11-slim, HEALTHCHECK curl на /health. `docker-compose.yml` — port 8000, volume ./artifacts, env_file configs/.env, healthcheck, restart unless-stopped. `.dockerignore`. README раздел 4.5: 3 варианта запуска. |
| 7 | Есть `.env.example` и **нет** в репозитории реальных секретов | ✅ | `configs/.env.example` — 4 шаблонные переменные (MODEL_PATH, API_HOST, API_PORT, LOG_LEVEL). `.gitignore:2` — `.env` в списке игнорируемых. `configs/.env` — только шаблонные значения, без паролей/токенов. |
| 8 | Реализованы логи/наблюдаемость (хотя бы консольные логи + `/health`) | ✅ | `src/api/main.py:19-24` — structured logging (timestamp, level, module, message). `/health` endpoint (строка 106-112): возвращает status, model_loaded, model_type. Dockerfile:46 — HEALTHCHECK. docker-compose.yml:10-15 — healthcheck с interval/timeout/retries. |
| 9 | В `report.md` **обоснован выбор финальной модели** по результатам | ✅ | `report.md` раздел 5 — таблица + 6 пунктов обоснования. **Финальный выбор:** YOLOv8n v4 (F1 = 0.754). Причины: (1) v4 — единственная воспроизводимая версия, (2) лучший F1 среди воспроизводимых, (3) CV baseline (F1 0.727) уступает, (4) Hybrid не улучшает YOLO, (5) RCNN последний (F1 0.627), (6) v3 невоспроизводим. |
| 10 | `README.md` и `report.md` позволяют понять сценарий демонстрации | ✅ | `README.md` раздел 4 — установка, запуск (3 варианта), тестирование. `report.md` раздел 9 — пошаговый сценарий защиты из 5 шагов (подготовка → запуск → демонстрация эндпоинтов → эксперименты → ключевые наблюдения). `README.md` раздел 5 — сводная таблица метрик. |
