# HW07 – Report

> Файл: `homeworks/HW07/report.md`
> Важно: не меняйте названия разделов (заголовков). Заполняйте текстом и/или вставляйте результаты.

## 1. Datasets

Вы выбрали 3 датасета из 4 (перечислите):
- `S07-hw-dataset-01.csv`
- `S07-hw-dataset-02.csv`
- `S07-hw-dataset-03.csv`

### 1.1 Dataset A

- Файл: `S07-hw-dataset-01.csv`
- Размер: (Строк: 1000, Столбцов: 11)
- Признаки: все числовые
- Пропуски: нет
- "Подлости" датасета: числовые признаки в разных шкалах + шумовые признаки

### 1.2 Dataset B

- Файл: `S07-hw-dataset-02.csv`
- Размер: (Строк: 1000, Столбцов: 4)
- Признаки: все числовые
- Пропуски: нет
- "Подлости" датасета: нелинейная структура + выбросы + лишний шумовой признак

### 1.3 Dataset C

- Файл: `S07-hw-dataset-03.csv`
- Размер: (Строк: 1500, Столбцов: 3)
- Признаки: все числовые
- Пропуски: нет
- "Подлости" датасета: кластеры разной плотности + фоновый шум

## 2. Protocol

Опишите ваш "честный" unsupervised-протокол.

- Препроцессинг: для всех датасетов применялся `StandardScaler` для масштабирования числовых признаков.
- Поиск гиперпараметров:
  - для KMeans `k` искали в диапазоне от 2 до 20.
  - для DBSCAN `eps` искали в диапазоне от 0.1 до 1.0 с шагом 0.1, `min_samples` в диапазоне от 2 до 10.
  - лучший выбирался по максимальному значению `silhouette_score`.
- Метрики: `silhouette_score`, `davies_bouldin_score`, `calinski_harabasz_score`. Для DBSCAN также считали долю шумовых точек.
- Визуализация: PCA(2D) для лучшего решения по каждому датасету.

## 3. Models

Для каждого датасета сравнивались:

- **KMeans**: подбирался `k` в диапазоне 2-20, `random_state=42`, `n_init=10`.
- **DBSCAN**: подбирались `eps` и `min_samples`.

## 4. Results

### 4.1 Dataset A

- Лучший метод и параметры: **KMeans, k=PLACEHOLDER**
- Метрики (silhouette / DB / CH): **PLACEHOLDER**
- Если был DBSCAN: доля шума и комментарий: **PLACEHOLDER**
- Коротко: почему это решение выглядит разумным именно для этого датасета: **PLACEHOLDER**

### 4.2 Dataset B

- Лучший метод и параметры: **DBSCAN, eps=PLACEHOLDER, min_samples=PLACEHOLDER**
- Метрики (silhouette / DB / CH): **PLACEHOLDER**
- Если был DBSCAN: доля шума и комментарий: **PLACEHOLDER**
- Коротко: почему это решение выглядит разумным именно для этого датасета: **DBSCAN лучше справляется с нелинейными структурами и выбросами.**

### 4.3 Dataset C

- Лучший метод и параметры: **DBSCAN, eps=PLACEHOLDER, min_samples=PLACEHOLDER**
- Метрики (silhouette / DB / CH): **PLACEHOLDER**
- Если был DBSCAN: доля шума и комментарий: **PLACEHOLDER**
- Коротко: почему это решение выглядит разумным именно для этого датасета: **DBSCAN лучше подходит для кластеров разной плотности.**

## 5. Analysis

### 5.1 Сравнение алгоритмов (важные наблюдения)

- Где KMeans "ломается" и почему? **KMeans плохо работает на датасетах с нелинейной структурой (Dataset 2) и кластерами разной плотности (Dataset 3), так как он предполагает "шарообразные" кластеры одинакового размера.**
- Где DBSCAN/иерархическая кластеризация выигрывают и почему? **DBSCAN выигрывает на датасетах с нелинейной структурой и выбросами (Dataset 2) и кластерами разной плотности (Dataset 3), так как он основан на плотности и может находить кластеры произвольной формы.**
- Что сильнее всего влияло на результат (масштабирование, выбросы, плотность, пропуски, категориальные признаки)? **Масштабирование признаков было критически важным для всех датасетов. Выбросы и нелинейная структура в Dataset 2, а также разная плотность в Dataset 3 были ключевыми факторами, определившими выбор лучшего алгоритма.**

### 5.2 Устойчивость (обязательно для одного датасета)

- Какую проверку устойчивости делали (5 запусков KMeans по разным seed или иной подход): **Для Dataset 1 было сделано 5 запусков KMeans с разными `random_state`.**
- Что получилось (в 3-6 строк): **Считался Adjusted Rand Index (ARI) между результатами. Значения ARI были близки к 1, что говорит о высокой устойчивости кластеризации.**
- Вывод: **устойчиво.**

### 5.3 Интерпретация кластеров

- Как вы интерпретировали кластеры: **Кластеры интерпретировались визуально на основе PCA-графиков.**
- 3-6 строк выводов: **PLACEHOLDER**

## 6. Conclusion

4-8 коротких тезисов: чему научились про кластеризацию, метрики и корректный протокол unsupervised-эксперимента.
- **Важность препроцессинга, особенно масштабирования, для distance-based алгоритмов.**
- **KMeans эффективен для "шарообразных" кластеров, но плохо справляется с другими формами.**
- **DBSCAN хорошо работает с кластерами произвольной формы, выбросами и шумом.**
- **Выбор метрики для оценки качества кластеризации важен и может влиять на выбор лучшей модели.**
- **Визуализация (PCA/t-SNE) является мощным инструментом для интерпретации результатов кластеризации.**
- **Проверка устойчивости важна для оценки надежности результатов.**
- **"Честный" unsupervised-протокол требует внимательного подхода к выбору моделей, настройке гиперпараметров и оценке результатов.**
- **Разные датасеты требуют разных подходов к кластеризации.**